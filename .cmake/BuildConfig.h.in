#pragma once

#define APPLICATION_NAME "${PROJECT_NAME}"
#define VERSION_NAME "${MAJOR_VERSION}.${MINOR_VERSION}.${PATCH_VERSION}"
#define BUILD_DATE_TIME "${BUILD_DATE_TIME}"
#define AUTHOR "${AUTHOR}"
#define REPOSITORY_URL "${REPOSITORY_URL}"
#define UPDATE_CHECK_URL "${UPDATE_CHECK_URL}"
#define UPDATE_URL "${UPDATE_URL}"
